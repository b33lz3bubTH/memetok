1. in super admin route, it will ask for super admin credentials in a modal first then it will allow setting uploaders and api keys for each individual uploaders, and revoke keys.
2. create a proper db schema for these. and also check the backend for this.